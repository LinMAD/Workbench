app:
    resource: '@AppBundle/Controller/'
    type: annotation

movies:
  resource: AppBundle\Controller\Api\MovieController
  type:     annotation
  defaults: { "_format" : "json" }
  prefix: /api
